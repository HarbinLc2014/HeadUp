/**
 * @format
 */

import {AppRegistry} from 'react-native';
import HeadUpApp from './HeadUpApp'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => HeadUpApp);
